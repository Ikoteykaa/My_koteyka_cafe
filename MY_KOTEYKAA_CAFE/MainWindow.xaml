<Window x:Class="MY_KOTEYKAA_CAFE.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="☕ Monks Cafe - Billing System" Height="678" Width="850" 
        WindowStartupLocation="CenterScreen" Background="#F3F4F6" FontFamily="Segoe UI">

    <Window.Resources>

        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Padding" Value="15"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#000000" Direction="270" ShadowDepth="2" BlurRadius="10" Opacity="0.05"/>
                </Setter.Value>
            </Setter>
        </Style>


        <Style x:Key="HeaderStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#1F2937"/>
            <Setter Property="Margin" Value="0,0,0,12"/>
        </Style>

        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#4B5563"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Margin" Value="0,0,0,4"/>
        </Style>


        <Style TargetType="TextBox">
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="Margin" Value="0,0,0,12"/>
            <Setter Property="BorderBrush" Value="#D1D5DB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="6">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>


        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="#3B82F6"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="0,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#2563EB"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="DangerButton" TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
            <Setter Property="Background" Value="#EF4444"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#DC2626"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="SecondaryButton" TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
            <Setter Property="Background" Value="#E5E7EB"/>
            <Setter Property="Foreground" Value="#374151"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#D1D5DB"/>
                </Trigger>
            </Style.Triggers>
        </Style>

    </Window.Resources>

    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="280" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0" Margin="0,0,20,0">

            <Border Style="{StaticResource CardStyle}">
                <StackPanel>
                    <TextBlock Text="🛒  Add Item" Style="{StaticResource HeaderStyle}"/>

                    <TextBlock Text="Description (3-20 symbols)" />
                    <TextBox x:Name="txtDesc" Height="45" />

                    <TextBlock Text="Price ($)" />
                    <TextBox x:Name="txtPrice" Height="45" />

                    <Button Content="Add to Bill" Style="{StaticResource PrimaryButton}" Click="BtnAdd_Click"/>
                </StackPanel>
            </Border>

            <Border Style="{StaticResource CardStyle}">
                <StackPanel>
                    <TextBlock Text="💰 Add Tip" Style="{StaticResource HeaderStyle}"/>

                    <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                        <RadioButton x:Name="rbPercent" Content="Percent (%)" IsChecked="True" Margin="0,0,15,0" Foreground="#4B5563"/>
                        <RadioButton x:Name="rbAmount" Content="Amount ($)" Foreground="#4B5563"/>
                    </StackPanel>

                    <TextBox x:Name="txtTip" Height="45" />

                    <UniformGrid Columns="2" Rows="1" Margin="0,5,0,0">
                        <Button Content="Apply Tip" Style="{StaticResource PrimaryButton}" Margin="0,0,5,0" Click="BtnApplyTip_Click"/>
                        <Button Content="No Tip" Style="{StaticResource SecondaryButton}" Margin="5,0,0,0" Click="BtnNoTip_Click"/>
                    </UniformGrid>
                </StackPanel>
            </Border>

            <Border Style="{StaticResource CardStyle}">
                <StackPanel>
                    <TextBlock Text="📁 Save / Load Bill" Style="{StaticResource HeaderStyle}"/>

                    <TextBlock Text="Filename (1-10 symbols)" />
                    <TextBox x:Name="txtFileName" Height="45" />

                    <UniformGrid Columns="2" Rows="1" Margin="0,5,0,0">
                        <Button Content="Save" Style="{StaticResource SecondaryButton}" Margin="0,0,5,0" Click="BtnSave_Click"/>
                        <Button Content="Load" Style="{StaticResource SecondaryButton}" Margin="5,0,0,0" Click="BtnLoad_Click"/>
                    </UniformGrid>
                </StackPanel>
            </Border>
        </StackPanel>

        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Style="{StaticResource CardStyle}" Padding="0" ClipToBounds="True">
                <DataGrid x:Name="dgItems" AutoGenerateColumns="False" CanUserAddRows="False" 
                          IsReadOnly="True" HeadersVisibility="Column" 
                          Background="White" BorderThickness="0" GridLinesVisibility="Horizontal" 
                          HorizontalGridLinesBrush="#F3F4F6" RowHeight="35">

                    <DataGrid.Resources>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="#F9FAFB"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                            <Setter Property="Foreground" Value="#6B7280"/>
                            <Setter Property="Padding" Value="10,8"/>
                            <Setter Property="BorderThickness" Value="0,0,0,1"/>
                            <Setter Property="BorderBrush" Value="#E5E7EB"/>
                        </Style>
                        <Style TargetType="DataGridCell">
                            <Setter Property="Padding" Value="10,0"/>
                            <Setter Property="VerticalContentAlignment" Value="Center"/>
                            <Setter Property="BorderThickness" Value="0"/>
                        </Style>
                    </DataGrid.Resources>

                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Item Description" Binding="{Binding Description}" Width="*" />
                        <DataGridTextColumn Header="Price" Binding="{Binding Price, StringFormat=C2}" Width="120">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="FontWeight" Value="Medium"/>
                                    <Setter Property="HorizontalAlignment" Value="Right"/>
                                    <Setter Property="Margin" Value="0,0,10,0"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>

            <StackPanel Grid.Row="1">

                <UniformGrid Columns="2" Rows="1" Margin="0,0,0,15">
                    <Button Content="Remove Selected &#xD;&#xA;&#xD;&#xA;" Style="{StaticResource SecondaryButton}" Margin="0,0,8,0" Click="BtnRemove_Click" Height="21"/>
                    <Button Content="Clear Entire Bill " Style="{StaticResource DangerButton}" Margin="8,0,0,0" Click="BtnClear_Click"/>
                </UniformGrid>

                <Border Style="{StaticResource CardStyle}" Background="#1F2937" Margin="0">
                    <StackPanel>
                        <TextBlock Text="Bill Summary" Foreground="White" FontSize="18" FontWeight="Bold" Margin="0,0,0,15" HorizontalAlignment="Center"/>

                        <Grid Margin="0,0,0,8">
                            <TextBlock Text="Net Total:" Foreground="#9CA3AF" FontSize="15"/>
                            <TextBlock x:Name="lblNetTotal" Text="$0.00" Foreground="White" HorizontalAlignment="Right" FontSize="15"/>
                        </Grid>

                        <Grid Margin="0,0,0,8">
                            <TextBlock Text="Tip Amount:" Foreground="#9CA3AF" FontSize="15"/>
                            <TextBlock x:Name="lblTip" Text="$0.00" Foreground="White" HorizontalAlignment="Right" FontSize="15"/>
                        </Grid>

                        <Grid Margin="0,0,0,15">
                            <TextBlock Text="GST Amount (5%):" Foreground="#9CA3AF" FontSize="15"/>
                            <TextBlock x:Name="lblGst" Text="$0.00" Foreground="White" HorizontalAlignment="Right" FontSize="15"/>
                        </Grid>

                        <Border BorderBrush="#374151" BorderThickness="0,1,0,0" Margin="0,0,0,15"/>

                        <Grid>
                            <TextBlock Text="TOTAL AMOUNT" Foreground="White" FontWeight="Bold" FontSize="20"/>
                            <TextBlock x:Name="lblTotal" Text="$0.00" Foreground="#34D399" FontWeight="Bold" HorizontalAlignment="Right" FontSize="22"/>
                        </Grid>
                    </StackPanel>
                </Border>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
